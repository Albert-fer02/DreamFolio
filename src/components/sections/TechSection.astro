---
import {
  Blocks,
  Database,
  FileJson,
  Globe,
  Layers,
  ShieldCheck,
  Sparkles,
  Wind,
} from 'lucide-react'

const stackItems = [
  {
    title: 'Astro 5',
    label: 'framework',
    detail: 'Static-first architecture to eliminate runtime overhead and maximize edge security.',
    icon: Globe,
  },
  {
    title: 'React 19',
    label: 'islands',
    detail: 'Selective hydration strategy, executing JavaScript only where interactivity is mission-critical.',
    icon: Blocks,
  },
  {
    title: 'Tailwind v4',
    label: 'styling',
    detail: 'Tokenized design system ensuring consistent UI integrity and low-latency delivery.',
    icon: Wind,
  },
  {
    title: 'Content Collections',
    label: 'data',
    detail: 'Type-safe project schemas that guarantee data consistency and architectural order.',
    icon: Database,
  },
  {
    title: 'Structured Data',
    label: 'seo',
    detail: 'Schema.org implementation to ensure deep indexing and algorithmic clarity.',
    icon: FileJson,
  },
  {
    title: 'Static Hosting',
    label: 'delivery',
    detail: 'Deployment targets optimized for immutability and atomic updates.',
    icon: Layers,
  },
  {
    title: 'Accessibility',
    label: 'ux',
    detail: 'A11y-compliant surfaces with contrast discipline and reduced-motion respect.',
    icon: ShieldCheck,
  },
  {
    title: 'Design System',
    label: 'visual',
    detail: 'High-fidelity visual language using OLED black and silver for maximum signal.',
    icon: Sparkles,
  },
]
---

<section id="tech" class="relative border-t border-white/6 bg-black/35 py-16 sm:py-20 lg:py-24">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex flex-col gap-6 lg:grid lg:grid-cols-[minmax(0,1.15fr)_minmax(0,1.85fr)] lg:items-end">
      <div>
        <p class="font-code text-[11px] uppercase tracking-[0.2em] text-zinc-500">
          Frontend architecture
        </p>
        <h2 class="mt-3 text-3xl sm:text-4xl md:text-5xl font-display font-bold text-white tracking-[-0.03em]">
          Built for static clarity,
          <span class="gradient-text-financial"> not framework theater</span>
        </h2>
      </div>
      <p class="max-w-2xl text-base leading-8 text-zinc-300 lg:ml-auto">
        The stack is intentionally simple: static-first rendering, typed content,
        restrained hydration, and a UI system that prioritizes legibility over novelty.
      </p>
    </div>

    <div class="mt-12 grid grid-cols-1 gap-4 md:grid-cols-2 xl:grid-cols-4">
      {
        stackItems.map((item) => {
          const Icon = item.icon

          return (
            <article class="hover-soft rounded-[1.6rem] border border-white/8 bg-white/[0.02] p-4 sm:p-6 backdrop-blur-sm">
              <div class="flex items-center justify-between">
                <div class="rounded-2xl border border-white/8 bg-white/[0.025] p-3 text-zinc-100">
                  <Icon size={20} strokeWidth={1.5} />
                </div>
                <span class="font-code text-[10px] uppercase tracking-[0.18em] text-zinc-500">
                  {item.label}
                </span>
              </div>

              <h3 class="mt-5 text-xl font-display font-semibold text-white">
                {item.title}
              </h3>
              <p class="mt-3 text-sm leading-7 text-zinc-400">
                {item.detail}
              </p>
            </article>
          )
        })
      }
    </div>
  </div>
</section>
